/**
 * No custom field resolvers. Every type this plugin exposes is built in its
 * query resolvers from values already in hand, so there is nothing to resolve
 * lazily per field.
 */
export const customResolvers = {};
